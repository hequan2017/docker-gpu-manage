# 天启算力管理平台 - 官网

这是天启算力管理平台的官方网站，采用纯 HTML/CSS/JavaScript 开发，具有科技感的视觉设计。

## 📁 目录结构

```
website/
├── index.html          # 主页
├── css/
│   └── style.css       # 样式文件
├── js/
│   └── main.js         # 交互脚本
├── images/             # 图片资源（待添加）
├── assets/             # 其他资源（待添加）
└── README.md           # 本文件
```

## 🎨 设计特点

- **科技风格**: 采用深色主题，配合青色和紫色的渐变效果
- **响应式设计**: 完美适配桌面端、平板和移动设备
- **动画效果**: 丰富的交互动画和过渡效果
- **无依赖**: 纯原生开发，无需任何前端框架

## 🚀 快速开始

### 本地预览

1. 使用任何静态文件服务器：

```bash
# 使用 Python
cd website
python -m http.server 8000

# 使用 Node.js (http-server)
npx http-server website -p 8000

# 使用 PHP
php -S localhost:8000 -t website
```

2. 在浏览器中访问 `http://localhost:8000`

### 直接打开

也可以直接在浏览器中打开 `index.html` 文件。

## 📝 页面结构

### 主页 (index.html)

- **导航栏**: 固定顶部，支持滚动效果和移动端菜单
- **主视觉区**: 产品介绍、核心价值和统计数据
- **核心功能**: 6 大核心功能模块展示
- **技术架构**: 前端、后端技术栈可视化展示
- **应用场景**: 6 大应用场景介绍
- **快速部署**: Docker、Kubernetes、本地开发三种部署方式
- **CTA 区域**: 引导用户开始使用
- **页脚**: 链接和版权信息

## 🎯 主要功能

### 交互功能

- ✅ 平滑滚动导航
- ✅ 移动端响应式菜单
- ✅ 数字动画计数
- ✅ 标签页切换
- ✅ 代码复制功能
- ✅ 滚动到顶部按钮
- ✅ 卡片悬浮效果
- ✅ 键盘快捷键支持

### 动画效果

- ✅ 背景粒子动画
- ✅ 网格背景动画
- ✅ GPU 卡片 3D 旋转
- ✅ 渐入动画
- ✅ 悬浮效果

## 🎨 颜色主题

```css
--primary-color: #00f0ff;     /* 主色调：青色 */
--secondary-color: #7b2cbf;   /* 辅助色：紫色 */
--bg-primary: #0a0e27;        /* 主背景 */
--bg-secondary: #141832;      /* 次背景 */
--text-primary: #ffffff;      /* 主文字 */
--text-secondary: #a0aec0;    /* 次文字 */
```

## 📱 响应式断点

- **桌面端**: > 1024px
- **平板端**: 768px - 1024px
- **移动端**: < 768px
- **小屏移动端**: < 480px

## 🔧 自定义配置

### 修改主题颜色

编辑 `css/style.css` 中的 CSS 变量：

```css
:root {
    --primary-color: #00f0ff;  /* 修改为你喜欢的颜色 */
    /* ... */
}
```

### 修改内容

直接编辑 `index.html` 中的文本内容即可。

### 添加图片

1. 将图片放入 `images/` 目录
2. 在 HTML 中引用：`<img src="images/your-image.png" alt="描述">`

## 🌐 部署

### 静态网站托管

可以部署到任何静态网站托管服务：

- GitHub Pages
- Netlify
- Vercel
- Cloudflare Pages
- 阿里云 OSS
- 腾讯云 COS

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/website;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 缓存静态资源
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

## 📊 性能优化

- ✅ 最小化 HTTP 请求
- ✅ CSS 和 JavaScript 压缩（生产环境）
- ✅ 图片懒加载（可添加）
- ✅ 浏览器缓存
- ✅ CDN 加速（可选）

## 🛠️ 开发建议

### 添加新页面

1. 在 `website/` 目录下创建新的 HTML 文件
2. 复制 `index.html` 的基本结构
3. 修改内容和样式
4. 在导航栏中添加链接

### 添加新功能

1. 在 `js/main.js` 中添加新的 JavaScript 代码
2. 在 `css/style.css` 中添加对应的样式
3. 在 `index.html` 中添加相应的 HTML 结构

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

- GitHub: https://github.com/he7555/docker-gpu-manage
- 邮箱: support@gpu-manage.io
